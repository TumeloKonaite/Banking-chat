# Banking RAG CDK

This folder contains the AWS CDK app for deploying the Banking RAG demo.

## Quickstart

```bash
cd infra-cdk
python -m venv .venv
source .venv/bin/activate  # on Windows: .venv\\Scripts\\Activate.ps1
pip install -r requirements.txt
cdk synth
```
